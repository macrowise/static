backend:
  name: git-gateway
  branch: __BRANCH__

media_folder: "static/uploads"
public_folder: "/uploads"

collections:
  - name: "home"
    label: "Home"
    folder: "content/"
    create: false
    fields:
      - label: "Title"
        name: "title"
        widget: "string"
      - label: "Body"
        name: "body"
        widget: "markdown"
          